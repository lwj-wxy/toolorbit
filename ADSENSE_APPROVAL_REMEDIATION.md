# ToolOrbit AdSense 过审整改清单

版本：v1.0
日期：2026-06-18
目标：让 ToolOrbit 通过 Google AdSense 审核
适用阶段：过审优先（先于 AI_TOOLS_UPGRADE_PRD.md 的 Runtime 工程）

---

## 0. 一句话主线

> **AdSense 审核员不看后端，只看"一个真实访客打开这个页面，有没有得到有价值、原创、像人写的内容"。**
> 在过审之前，所有不直接改变"访客在页面上看到什么"的工程（Runtime、ContentBrief、缓存、限流、错误码）一律暂停或降级。

被拒多次 + 内容其实不薄 → 说明问题不是"内容量"，而是**内容质量信号**：AI 模板化痕迹、工具页对人的价值不足、以及部分合规/可信信号缺位。本清单只解决这三件事。

---

## 1. 诊断结论（为什么被拒）

基于当前项目事实：79 个工具、52 篇博客、about/privacy/terms/authors 齐全、但工具页 Google 索引率仅 36.9%、中文页 36.2%。

| # | 根因 | 证据 | 严重度 |
|---|---|---|---|
| D1 | 工具页 overview 是**给开发者看的实现描述**，对访客零价值 | `processing` 字段写满 "fetch POST 到 /api/...""模型只返回 JSON""FileReader 读取 data URL" | 致命 |
| D2 | 79 个工具页**同构模板**，易被判为程序化批量薄内容 | 所有工具同一套 summary/input/output/processing/modes/example 结构 | 致命 |
| D3 | 大量页面 **Google 未收录**（索引率 ~37%） | `docs/seo-tool-page-quality-prd.md` 的 GSC 快照 | 高 |
| D4 | 文案有明显 **AI 生成口吻**（堆砌、套话、缺真实视角） | overview 通篇陈述功能，无真实使用经验、无作者观点 | 高 |
| D5 | 现行 `AI_TOOLS_UPGRADE_PRD.md` **把团队资源引向后端工程**，与过审目标正交 | 全文 0 次提及 AdSense / 访客价值 / E-E-A-T | 高（机会成本）|

> 核心矛盾：现有 PRD 第 6.1 节已写"前台不要出现 fetch/SSE/JSON.parse"，但**存量 79 个工具页的 `processing` 字段恰恰全是这些**。整改第一刀就砍这里。

---

## 2. 过审 P0 范围（只做这些）

### 2.1 必做

1. **重写工具页"处理方式/processing"字段**：从实现描述改为**用户视角的"它怎么帮你、注意什么"**。删除所有 fetch / POST / JSON / FileReader / data URL / setState / 接口路径 等工程词。
2. **去模板化**：让首批工具页读起来像人针对**这一个工具**写的，而非套模板填空。每页加入该工具独有的真实使用场景、限制、常见错误。
3. **收敛工具总数 / 设置 noindex**：把 79 个工具中**质量不达标、无独立价值**的页面临时 `noindex` 或下线，只留下一批**精修过的强页面**面对审核。宁可 20 个好页，不要 79 个薄页。
4. **补强 E-E-A-T 可信信号**：作者页要有真实简介，工具页标注"谁维护、何时更新、数据来源/免责声明"。
5. **首页 + Hub 给真实导览**：`/` 和 `/ai-tools` 首屏要有面向人的介绍段落和清晰分类入口，不是工具墙。

### 2.2 不做（过审阶段明确暂停）

- 不做 AI Tool Runtime 配置化（`tool-config.ts` / ContentBrief / `runTextTool`）。
- 不做缓存 key / contentBriefHash / Redis 限流。
- 不做完整错误码体系、SSE→JSON 迁移。
- 不做 11 个埋点事件、Golden Set 离线评测。
- 不做 P1/P2 工具的双语 SEO 基线表。

> 这些是好工程，但对"通过审核"零贡献。过审后再回到 `AI_TOOLS_UPGRADE_PRD.md` 推进。

---

## 3. 工具页内容改造规范（最关键一节）

每个面向审核的工具页，正文（SSR 可读内容）必须满足：

### 3.1 必须删除（AI 痕迹 / 低价值信号）

- 任何接口路径、HTTP 方法、`fetch`、`POST`、`/api/...`。
- `JSON`、`JSON.parse`、`SSE`、`FileReader`、`data URL`、`base64`、`setState`、组件名、CSS 类名。
- "模型被约束为只返回 JSON""前端解析后渲染"之类实现叙述。
- 空泛套话："强大的""一站式""轻松搞定""智能化赋能"。

### 3.2 必须包含（对人有价值）

| 区块 | 要求 | 反例 → 正例 |
|---|---|---|
| 它解决什么问题 | 具体场景，不是功能罗列 | "把商品信息整理成 JSON" → "上架前 30 分钟，你手里只有一堆零散卖点，需要快速生成可直接粘进 Amazon 后台的标题和五点描述" |
| 怎么用（3-4 步） | 用户动作，不是数据流 | "前端 POST 到 /api" → "1. 填入产品名和 2-3 个核心卖点 2. 选目标平台 3. 点生成 4. 逐条复制需要的部分" |
| 输出里有什么 / 怎么用 | 站在拿到结果的人角度 | 列出每块输出 + **该怎么二次加工**、何时别直接用 |
| 限制与注意 | 真实边界，建立信任 | "不联网，不提供实时销量；HS 编码方向仅供和报关行沟通，最终以官方税则为准" |
| 1 个真实示例 | 具体输入 → 具体输出 + 一句点评 | 保留现有 example，但加一句"为什么这个输出可用 / 还需要改什么" |
| 维护与来源 | E-E-A-T | "由 ToolOrbit 编辑团队维护，最近更新 2026-06；平台规则为人工整理基线，非官方完整规则" |

### 3.3 文件落点（沿用现有结构，不引入 Runtime）

- 工具 overview：`src/views/tools/ai/data.ts`（直接改 `processing` 等字段的文案，**不动数据结构**）
- 非 AI 工具说明：`src/components/ToolContent.tsx` 对应数据源
- 标题/描述：`src/locales/en.json` / `src/locales/zh.json`
- 注意：中文页不要做英文直译，用自然中文搜索词（这也是中文页索引率低的原因之一）

---

## 4. 工具页"留多少 / noindex 多少"决策

索引率 37% 说明 Google 已经在用脚投票。与其让 79 个薄页拖低全站质量评分，不如：

1. 选出 **15-25 个**有真实独立搜索价值、且能精修到位的工具，作为"过审展示面"。
2. 其余工具页临时 `noindex,follow`（保留站内可用，但不计入质量评估面）。
3. 审核通过后，分批精修剩余工具，达标一个放开一个 `index`。

> 优先精修候选（电商/AI 主线，搜索意图清晰）：Listing Generator、YouTube Generator、Keyword Analyzer、Text Polisher、Translator、Excel Formula、Regex、PDF 类高频工具。
> 这一步同时改善 `seo-tool-page-quality-prd.md` 关注的索引率问题——**和 AdSense 是同一个病根**。

---

## 5. 站点级合规 / 可信检查

| 项 | 现状 | 待办 |
|---|---|---|
| Privacy Policy | 已有 `/privacy` | 确认含 cookie / 第三方广告（AdSense/Google）数据使用条款 |
| Terms | 已有 `/terms` | 确认可访问、有实质内容 |
| About | 已有 `/about` | 写清"我们是谁、做什么、为什么可信"，非占位 |
| Authors | 已有 `/authors/*` | 作者要有真实简介、领域、（最好）头像，支撑 E-E-A-T |
| Contact | 待确认 | **AdSense 偏好有可联系方式**；若无，加联系页/邮箱 |
| 导航 | 已有 navigation-menu | 确认页脚有 Privacy/Terms/About/Contact 链接 |
| 广告位 | — | 审核期可放 AdSense 代码，但**别堆假广告位/占位**；内容优先 |
| 原创性 | 风险点 | 抽查工具页/博客，确保不是 AI 套模板批量产物 |

---

## 6. 博客（52 篇）抽查

博客索引率也仅 37%。抽查 5-8 篇代表作，判断：是否真实原创、是否套模板、是否有作者观点和具体信息。若多数是 AI 泛文，**先 noindex 弱文，精修少量强文**，逻辑同工具页。

---

## 7. 执行顺序

1. 选定 15-25 个"展示面"工具，其余 `noindex,follow`。
2. 重写展示面工具的 `processing` 等字段：删工程词，按 §3.2 补人话内容。
3. 中文页改自然中文表达，非直译。
4. 工具页加"维护/来源/免责"信息（E-E-A-T）。
5. 补 Contact 页 + 核对 Privacy 含广告条款。
6. 首页/`/ai-tools` 首屏加面向人的导览段落。
7. 抽查博客，弱文 noindex。
8. 本地核对渲染效果（看真实页面，不是看代码）。
9. 重新提交 AdSense；同时在 GSC 对展示面页面请求收录。

---

## 8. 验收标准（面向审核员视角）

- 随机打开任一展示面工具页，**正文没有一个工程术语**（fetch/JSON/API/setState…）。
- 每个展示面工具页能回答："它帮谁解决什么、怎么用、输出怎么用、有什么限制"。
- 两个不同工具页**读起来不像同一个模板填的**。
- 工具页有可见的维护信息 / 免责声明 / 数据来源说明。
- Privacy 含第三方广告数据条款；有 Contact 入口。
- 首页/Hub 首屏有人可读的介绍，不是纯工具墙。
- 展示面页面在 GSC 中可被收录（索引率明显上升）。

---

## 9. 与 AI_TOOLS_UPGRADE_PRD.md 的关系

- **本文件优先**。过审前，团队按本清单工作。
- `AI_TOOLS_UPGRADE_PRD.md` 不删除，**降级为过审后的工程演进参考**。其 Runtime / ContentBrief / 缓存 / 限流 / 错误码在过审后再做。
- 注意：本清单 §3 对工具页文案的"去工程化"要求，与原 PRD 第 6.1、6.1 节的文案纪律一致——只是原 PRD 没把它当成**过审主线**，且没覆盖到 79 个存量页面。
